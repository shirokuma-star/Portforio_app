<h1>ユーザー一覧</h1>

<div class="page">
<%= will_paginate %>
</div>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <h3><%= link_to user.name, user %></h3>
    </li>
  <% end %>
</ul>

<div class="page">
<%= will_paginate %>
</div>

<div class="center jumbotron">
    <%= link_to "TOP(ログイン)ページに戻る", login_path, class: "btn btn-lg btn-primary" %> 
</div>